---
name: {{skill-name}}
description: "TODO (install-time helper trigger): describe when this helper applies during install — e.g. \"detect/verify <something> while installing\". This is an install-time HELPER (active while the agent works the relevant scope), not a pull-UX advisor and not a delivered payload skill."
---

# {{skill-name}}

> **Stub — fill this in.** The CLI scaffolded this install-time helper skill's shape; you write the real
> description (above) and body (below). Replace every `TODO` before shipping.

This skill is a helper the executing agent uses **during install**. It is inert in the repo and activates only
when its scope is scanned (the root or a bundle's `installer-skills/`).

## What it does

TODO — describe the install-time helper behavior (e.g. probe for a tool, run a smoke test, normalize an
environment detail) and when the agent should reach for it.
