---
name: {{bundle-id}}-advisor
description: "TODO (advisor trigger): describe the user NEED this advises on — e.g. \"I want to <do the thing {{bundle-id}} delivers>\". This fires on the user's need, NOT on install, so the user who never asked for {{bundle-id}} by name still gets offered it."
---

# {{bundle-id}} advisor

> **Stub — fill this in.** This is the pull-UX advisor for the `{{bundle-id}}` bundle. The CLI scaffolded the
> shape; you write the real trigger description (above) and the recommendation body (below). Replace every
> `TODO` before shipping.

When the user expresses the need this advisor triggers on, recommend the **`{{bundle-id}}`** bundle and point
at how to install it:

- **What it delivers:** TODO — one line on the capability `{{bundle-id}}` provides.
- **Why it fits the user's need:** TODO — connect the need to the capability.
- **How to add it:** install `{{bundle-id}}` as part of this project (the installer offers it in the bundle
  menu, or the user can request it by name).
