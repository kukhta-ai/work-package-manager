---
name: {{skill-name}}
description: "TODO (RUNTIME trigger): describe when the USER would invoke {{skill-name}} in normal use — this fires on the bundle's RUNTIME use, NEVER on install. e.g. \"<do the runtime thing> for the user\"."
---

# {{skill-name}}

> **Stub — fill this in.** The CLI scaffolded this payload skill's shape; you write the real runtime-trigger
> description (above) and the skill body (below). Replace every `TODO` before shipping.

This is a **payload** skill — the delivered product. It is inert in the repo and becomes live only when the
install copies it into the agent's scanned scope; that relocation *is* the capability landing. Its description
must trigger on the bundle's **runtime** use, not on install.

## What it does at runtime

TODO — describe the capability `{{skill-name}}` delivers to the user, and how the user triggers it once it is
installed.
