# Delivered content → environment: authoritative reference files (code, data, config). NOT a guaranteed-
# verbatim placement — the receipt checksums what is actually placed, so divergence is detected (doc 06/07).
