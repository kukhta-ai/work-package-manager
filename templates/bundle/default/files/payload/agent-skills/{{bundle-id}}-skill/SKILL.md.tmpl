---
name: {{bundle-id}}-skill
description: ⟨The runtime capability the {{bundle-id}} bundle delivers — write a description that triggers on the user's RUNTIME need for it, never on "install".⟩ Use when ⟨the situation in which this capability should fire⟩.
---

# {{bundle-id}}-skill

This is the **payload skill** the `{{bundle-id}}` bundle delivers — the runtime product. It is inert in the
repository and becomes live only when the install copies it into the agent's scanned scope; that relocation is
the product landing (doc 06). Its description triggers on the bundle's *runtime* use, never on install.

⟨Replace this body with the actual capability: what the skill does, when to use it, and how. Keep the name
namespaced to the bundle so it cannot collide with another bundle's payload skill, and never place it in a bare
`skills/` directory.⟩
